Project where we download series of images in different formats and combine them all into sorted and searchable pdf.

Utilizes:
  1. requests library
  2. pytesseract library
  3. pillow image library

Requirements:
  brew install tesseract
